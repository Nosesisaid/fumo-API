// place files you want to import through the `$lib` alias in this folder.


export interface APIFumo {
    id: number;
    caption: string;
    img: string;
    attribution: string;
    involved: string[];
}